name: widget_book_view_lego
description: "Widget Book View powered by lego"
#publish_to: 'none'
repository: https://github.com/flutter-lego/widget_book_view_lego

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: #@add
  gap: #@add
  june_flow_util:
  styled_widget: #@add
  modal_bottom_sheet: #@add
  keyboard_attachable: #@add
  flutter_zoom_drawer: #@add
  flutter_screenutil:
  bot_toast:
  device_preview_minus: #@add


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  screenutil_lego: #@add
  bot_toast_lego: #@add

flutter:
  assets:
    - assets/lego/widget_book_view_lego/
  uses-material-design: true

topics:
  - lego